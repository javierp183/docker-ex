version: '3.0'
services:
  prometheus:
    image: prometheus:latest
    ports:
        - 9090:9090
    volumes:
      - ${PWD}/prometheus/prometheus.yml
  wordpress:
    image: wordpress:latest
    ports:
        - 80:80
  grafana:
    image: grafana/grafana
    ports:
        - 3000:3000
